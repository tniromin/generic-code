public class ship
{
  float hp;//health
  float damage;
  void getdata(float a, float b)
  { 
    hp = a;
    damag=b;
   }
}

class Dreednot//ship name dreadknote
{
  public static void main()
  {
  float=rating;
  ship Dn= new ship();//this class is registered a ship1 now as a subclass of ship
  Dn.getdata(2000,20);
  float COXA20=1000;//AVERAGE RATING
  rating=(Dn.hp+Dn.damage)/COXA20
  system.out.println("rating="+rating);
 }
}
